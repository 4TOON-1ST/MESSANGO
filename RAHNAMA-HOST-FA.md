# 📱 مِسنجیو (Messango) — پیام‌رسان وب حرفه‌ای

پیام‌رسان کامل شبیه تلگرام با Next.js + Socket.IO + Prisma (SQLite)

---

## ✨ امکانات

- 🔐 **ثبت‌نام و ورود** با نام کاربری، نام نمایشی و رمز عبور
- 💬 **چت خصوصی و گروهی** لحظه‌ای (Real-time با Socket.IO)
- 🎤 **ارسال ویس** (پیام صوتی با ویوفرم)
- 📞 **تماس صوتی (ویس‌کال)** دو نفره با WebRTC
- 🔊 **تماس صوتی گروهی در گروه‌ها** (WebRTC Mesh) — دکمه پیوستن، آواتار اعضا، قطع میکروفون، خروج
- 🖼 **ارسال عکس و فایل** (تا ۲۵ مگابایت) + لایت‌باکس
- ✍️ ریپلای، ویرایش، حذف، فوروارد پیام
- 👁 تیک خوانده‌شدن، نشانگر «در حال نوشتن»، آنلاین/آخرین بازدید
- 😀 پیکر ایموجی، جستجوی سراسری (کاربر، گروه، متن پیام‌ها)
- 🌙 **تم تیره/روشن + ۶ رنگ اکسنت** + آواتار و بیوگرافی (شخصی‌سازی کامل)
- 🛡 **پنل ادمین**: آمار و نمودار، مدیریت کاربران (مسدود/حذف/ارتقا/تغییر رمز)، مدیریت پیام‌ها، **پیام همگانی**
- 🔔 اعلان مرورگر + صدای پیام
- 📱 کاملاً ریسپانسیو (موبایل و دسکتاپ) و راست‌چین فارسی

---

## 👑 حساب ادمین پیش‌فرض

| | |
|---|---|
| نام کاربری | `admin` |
| رمز عبور | `Admin@1404` |

⚠️ بعد از استقرار، از پنل ادمین → کاربران → تغییر رمز عبور، رمز ادمین را عوض کنید.
(یا متغیرهای `ADMIN_USERNAME` و `ADMIN_PASSWORD` را قبل از اولین اجرا تنظیم کنید.)

بعد از ورود، از منوی پروفایل (بالا-راست) گزینه «پنل مدیریت» را بزنید.

---

## 🚀 استقرار روی Railway (رایگان) — قدم به قدم

### روش ۱: از طریق GitHub (پیشنهادی)

**قدم ۱ — آپلود سورس روی GitHub**
1. فایل ZIP خروجی را دانلود و باز کنید.
2. یک ریپازیتوری جدید در GitHub بسازید (Private هم می‌شود).
3. محتویات پوشه را آپلود کنید (Commit).

**قدم ۲ — ساخت پروژه در Railway**
1. بروید به [railway.app](https://railway.app) و با GitHub وارد شوید.
2. **New Project** → **Deploy from GitHub repo** → ریپازیتوری را انتخاب کنید.
   (اگر ریپو Private است، یک‌بار دکمه Authorize را بزنید.)

**قدم ۳ — تنظیم متغیرهای محیطی**
در پروژه → سرویس شما → تب **Variables** این‌ها را اضافه کنید:

| متغیر | مقدار | توضیح |
|---|---|---|
| `DATABASE_URL` | `file:/data/messenger.db` | دیتابیس روی Volume دائمی |
| `UPLOAD_DIR` | `/data/uploads` | فایل‌های ارسالی روی Volume |
| `JWT_SECRET` | یک رشته تصادفی طولانی | کلید امنیتی توکن‌ها (حتماً تغییر دهید!) |
| `ADMIN_USERNAME` | `admin` | نام کاربری ادمین |
| `ADMIN_PASSWORD` | رمز دلخواه قوی | رمز ادمین (قبل از اولین اجرا بگذارید) |
| `INTERNAL_SECRET` | یک رشته تصادفی دیگر | ارتباط داخلی ماژول‌ها |

**قدم ۴ — ساخت Volume (خیلی مهم!)**
1. روی سرویس راست‌کلیک → **Attach Volume** (یا تب Settings → Volumes → New Volume)
2. **Mount path** را بگذارید: `/data`
3. حجم 1GB برای شروع کافی است.

بدون Volume، با هر ری‌استارت، پیام‌ها و عکس‌ها پاک می‌شوند!

**قدم ۵ — دامنه**
تب **Settings** → Networking → **Generate Domain** → یک آدرس `*.up.railway.app` می‌گیرید. تمام! 🎉

Railway خودش `npm install && npm run build` را اجرا می‌کند و سرور با `node server.js` بالا می‌آید.

### روش ۲: CLI (اختیاری)

```bash
npm i -g @railway/cli
railway login
railway init
railway up
```

---

## 💻 اجرا روی کامپیوتر شخصی (VPS یا لوکال)

```bash
npm install          # نصب پکیج‌ها
npm run build        # بیلد پروژه
npm start            # اجرا روی پورت 3000
```

متغیرها را هم می‌توانید در فایل `.env` بگذارید:

```env
DATABASE_URL=file:./data/messenger.db
UPLOAD_DIR=./data/uploads
JWT_SECRET=یک-رشته-تصادفی-طولانی
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin@1404
```

سرور را با `pm2` همیشه‌روشن کنید:

```bash
npm i -g pm2
pm2 start server.js --name messango
pm2 save
```

---

## 🧪 تست سریع بعد از استقرار

1. با دو مرورگر (یا یک نرمال + یک ناشناس) دو حساب بسازید.
2. از لیست کاربران، برای همدیگه چت خصوصی بسازید.
3. پیام بفرستید → باید **بدون رفرش** در مرورگر دیگر ظاهر شود.
4. عکس بفرستید، دکمه میکروفون را بزنید و ویس ضبط کنید.
5. دکمه 📞 در بالای چت → تماس صوتی (اجازه میکروفون را بدهید).
6. یک **گروه** بسازید → دکمه 🔊 بالای گروه → «تماس صوتی گروهی»؛ اعضا با دکمه «پیوستن» اضافه می‌شوند، نوار تماس آواتار حاضرین و وضعیت میکروفون هرکدام را نشان می‌دهد و با دکمه قرمز خارج می‌شوید.
7. با حساب `admin` وارد شوید → منو → پنل مدیریت.

---

## ❓ سوالات پرتکرار

**تماس صوتی وصل نمی‌شود؟**
تماس‌ها P2P هستند. اگر هر دو طرف پشت فایروال سختگیرانه باشند ممکن است وصل نشود. روی Railway معمولاً در اکثر شبکه‌ها کار می‌کند. برای پوشش ۱۰۰٪ شبکه‌ها به سرور TURN (مثل coturn یا TURN ابری) نیاز دارید.

**تماس گروهی چند نفر را پشتیبانی می‌کند؟**
تماس گروهی به‌صورت Mesh برقرار می‌شود (صدا مستقیم بین اعضا ردوبدل می‌شود). برای کیفیت خوب، تا حدود **۸ نفر** ایده‌آل است؛ در تعداد بالاتر، پهنای‌باند و پردازش هر عضو به‌صورت نمایی زیاد می‌شود.

**فایل‌ها و دیتابیس پاک شدند؟**
یعنی Volume وصل نکرده‌اید یا مسیرها را اشتباه گذاشتید. `DATABASE_URL` باید حتماً داخل `/data` باشد.

**پورت چطور تنظیم می‌شود؟**
سرور خودش `process.env.PORT` را می‌خواند (Railway خودکار ست می‌کند). چیزی لازم نیست بکنید.

**می‌خواهم ثبت‌نام را ببندم؟**
فعلاً از پنل ادمین کاربران مزاحم را مسدود کنید. (قابلیت بستن ثبت‌نام در نسخه‌های بعد)

---

## 🏗 ساختار پروژه

```
├── server.js                  ← سرور پروداکشن (Next + Socket.IO یکجا)
├── server/
│   ├── auth-utils.js          ← هش رمز + JWT (بدون وابستگی)
│   └── socket-handler.js      ← منطق Real-time (پیام، حضور، تماس ۲نفره و گروهی)
├── src/
│   ├── app/api/               ← REST API (auth, conversations, upload, admin)
│   ├── components/messenger/  ← رابط کاربری کامل (+ GroupVoiceBar)
│   ├── lib/                   ← سوکت، وبرک، تماس گروهی، ضبط ویس، ابزارها
│   └── store/                 ← مدیریت state (Zustand)
├── prisma/schema.prisma       ← دیتابیس (SQLite)
└── railway.json               ← تنظیمات Railway
```
